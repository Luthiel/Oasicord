apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-service
  namespace: fashion-assistant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: social-service
  template:
    metadata:
      labels:
        app: social-service
    spec:
      containers:
        - name: social-service
          image: fashion-assistant/social-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - secretRef:
                name: social-service-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: social-service
  namespace: fashion-assistant
spec:
  selector:
    app: social-service
  ports:
    - port: 8083
      targetPort: 8083
