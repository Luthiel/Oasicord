apiVersion: apps/v1
kind: Deployment
metadata:
  name: wardrobe-service
  namespace: fashion-assistant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wardrobe-service
  template:
    metadata:
      labels:
        app: wardrobe-service
    spec:
      containers:
        - name: wardrobe-service
          image: fashion-assistant/wardrobe-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - secretRef:
                name: wardrobe-service-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: wardrobe-service
  namespace: fashion-assistant
spec:
  selector:
    app: wardrobe-service
  ports:
    - port: 8082
      targetPort: 8082
